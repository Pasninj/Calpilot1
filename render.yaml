services:
  - type: worker
    name: discord-copilot-bot
    env: python
    buildCommand: ./build.sh
    startCommand: python discord_copilot_bot.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false   # tu le mets dans Render UI
      - key: CHANNEL_ID
        sync: false   # pareil
